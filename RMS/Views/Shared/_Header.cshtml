@* Header partial view based on PoeGenerated.html *@
<header class="bg-primary text-white shadow-md">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
        <div class="flex items-center">
            <span class="text-2xl font-bold tracking-wide">Restaurant Manager</span>
        </div>
        <div class="flex items-center space-x-4">
            <span id="header-user-role" class="hidden md:inline font-semibold"></span>
            <button id="logout-btn" class="bg-secondary hover:bg-primary text-white px-4 py-2 rounded transition flex items-center">
                <i class="fas fa-sign-out-alt mr-1"></i>
                <span class="hidden md:inline">Đăng xuất</span>
            </button>
        </div>
    </div>
</header>
